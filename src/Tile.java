/* A tile is a one panel on the map grid, which will contain one of the following: House, Obstacle, or Road (empty space).
 * Also includes whether a tile is "passable" by the player or not. For instance, the player cannot pass through Houses or Obstacles, but
 * can pass through road tiles.
 * 
 * House, Obstacle, and Road classes extend this abstract class.
 */

public abstract class Tile {
	
	
}
